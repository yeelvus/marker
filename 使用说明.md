# Marker 本地部署与使用说明

部署位置：`/Users/wyattsun/Projects/marker`  
Conda 环境：`marker`（Python 3.12）  
已安装版本：Marker **2.0.0**（来自 [datalab-to/marker](https://github.com/datalab-to/marker)）

本机为 **Apple M1 Pro**，推荐使用 **`fast` 模式**（默认）。需要 OCR / 公式识别时依赖本机 **llama.cpp**（`llama-server`），已通过 Homebrew 安装。

旧版备份仍在：`/Users/wyattsun/Projects/marker-1.8.5`（不再使用该环境）。

---

## 一、启动图形界面（推荐）

在终端执行：

```bash
cd /Users/wyattsun/Projects/marker
./start_gui.sh
```

或：

```bash
conda activate marker
cd /Users/wyattsun/Projects/marker
python marker_gui.py
```

### 界面操作

1. 点击 **「选择文件…」** 选单个 PDF，或 **「选择文件夹…」** 批量处理目录内文档  
2. 确认 **输出目录**（默认 `marker/output`）  
3. 参数建议：
   - **模式**：`fast`（Mac 推荐）
   - **输出格式**：`markdown`
   - 纯电子 PDF、只要正文：勾选 **禁用 OCR**（最快）
   - 扫描件 / 乱码：勾选 **强制 OCR**
   - 内存紧张的批量：勾选 **逐个处理**
4. 点击 **「开始处理」**，等待完成  
5. 点击 **「打开输出目录」** 查看结果  

支持格式：`PDF`、图片、`DOCX`、`PPTX`、`XLSX`、`HTML`、`EPUB`（需已装 `marker-pdf[full]`）

---

## 二、命令行使用

```bash
conda activate marker
export SURYA_INFERENCE_BACKEND=llamacpp   # Mac 推荐
export PATH="/opt/homebrew/bin:$PATH"

# —— 封装脚本（推荐，与 GUI 相同逻辑）——
# 处理单个文件
python /Users/wyattsun/Projects/marker/process_docs.py \
  -p /path/to/file.pdf -o /path/to/output --mode fast

# 处理整个文件夹
python /Users/wyattsun/Projects/marker/process_docs.py \
  -p /path/to/pdf_folder -o /path/to/output --mode fast --workers 1

# 纯文本层、不启动 VLM（最快）
python /Users/wyattsun/Projects/marker/process_docs.py \
  -p /path/to/file.pdf -o /path/to/output --disable_ocr

# —— 官方 CLI ——
# 单文件
marker_single /path/to/file.pdf --output_dir ./output --mode fast

# 文件夹批量
marker /path/to/folder --output_dir ./output --mode fast --workers 1
```

### 常用参数

| 参数 | 说明 |
|------|------|
| `--mode fast\|balanced` | Mac 用 fast；更高精度用 balanced |
| `--output_format markdown\|json\|html\|chunks` | 输出格式 |
| `--disable_ocr` | 不调用 VLM，纯 PDF 文本层 |
| `--force_ocr` | 强制全页 OCR |
| `--use_llm` | LLM 增强（需配置 API Key） |
| `--skip_existing` | 批量时跳过已有输出 |
| `--workers N` | 并行 worker（Mac 建议 1） |

---

## 三、可选：官方 Streamlit / API

```bash
conda activate marker
pip install -U streamlit streamlit-ace uvicorn fastapi python-multipart

# Streamlit 交互界面
marker_gui

# FastAPI 服务
marker_server --port 8001
# 文档 http://localhost:8001/docs
```

---

## 四、输出说明

解析结果默认在输出目录下，按文件名分文件夹，常见内容包括：

| 文件 | 说明 |
|------|------|
| `*.md` | Markdown 正文 |
| `*_meta.json` / metadata | 目录、页统计等元数据 |
| 图片文件 | 从 PDF 提取的插图 |

---

## 五、LLM 增强（可选，默认不需要）

**不勾选「使用 LLM 增强」也能正常转 Markdown**，本地模型已足够日常使用。  
`--use_llm` 会调用云端/本地大模型做表格合并、公式修正等，**必须先配置 API Key**，否则会报：

```text
AssertionError: In order to use GoogleGeminiService, you must set ... gemini_api_key
```

### 配置 Gemini（默认 LLM 后端）

1. 打开 https://aistudio.google.com/apikey 申请密钥  
2. 任选一种方式写入密钥：

```bash
# 方式 A：环境变量（当前终端有效）
export GOOGLE_API_KEY="你的密钥"

# 方式 B：local.env（推荐，GUI / 脚本自动读取）
cd /Users/wyattsun/Projects/marker
cp local.env.example local.env
# 编辑 local.env，填入：GOOGLE_API_KEY=你的密钥
```

3. GUI 中也可在 **「Gemini Key」** 输入框直接粘贴（仅当次会话）  
4. 再勾选「使用 LLM 增强」开始处理  

```bash
# 命令行示例
python process_docs.py -p file.pdf -o ./output --mode fast --use_llm
# 或显式传 key
python process_docs.py -p file.pdf -o ./output --use_llm --gemini_api_key "你的密钥"
```

### 其他 LLM 后端

| 服务 | 参数 | 需要 |
|------|------|------|
| Gemini（默认） | `--llm_service gemini` | `GOOGLE_API_KEY` |
| OpenAI 兼容 | `--llm_service openai` | `OPENAI_API_KEY` |
| Ollama 本地 | `--llm_service ollama` | 本机 `ollama serve` + 模型 |
| OpenRouter | `--llm_service openrouter` | `OPENROUTER_API_KEY` |

---

## 六、注意事项

1. **首次运行**会下载 Surya / 布局等模型，体积较大，请保持网络畅通。  
2. **OCR / 公式**依赖 `llama-server`：`brew install llama.cpp`，并保证 `/opt/homebrew/bin` 在 `PATH` 中。  
3. 系统自带 / 其他 Python 可能不兼容，请务必 `conda activate marker`（Python 3.12）。  
4. 内存建议 16GB+；长文档若卡顿或 OOM，使用 `--mode fast --workers 1`，或勾选「逐个处理」/「禁用 OCR」。  
5. 磁盘空间较紧时，注意模型缓存目录（通常在 `~/.cache`）。  
6. 停止处理：GUI 点「停止」，或终端 `Ctrl+C`。  
7. **不要随便勾选 LLM**：没有 Key 会直接失败；日常转换请保持关闭。  

---

## 七、环境维护

```bash
# 激活环境
conda activate marker

# 升级 Marker（从本地仓库）
cd /Users/wyattsun/Projects/marker
git pull
pip install -e ".[full]"

# 或从 PyPI 升级
pip install -U "marker-pdf[full]"

# 查看入口
which marker_single marker
marker_single --help
```

---

## 七、与旧版 marker-1.8.5 的区别

| 项目 | 旧版 1.8.5 | 当前 2.0 |
|------|-----------|---------|
| 路径 | `Projects/marker-1.8.5` | `Projects/marker` |
| 模式 | 无 `--mode` | `fast` / `balanced` |
| OCR 后端 | 旧 Surya 管线 | 本地 llama.cpp / vLLM 推理服务 |
| 推荐入口 | `marker_single` | GUI / `process_docs.py` / 官方 CLI |
